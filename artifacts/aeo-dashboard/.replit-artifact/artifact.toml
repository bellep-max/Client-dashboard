kind = "web"
previewPath = "/"
title = "AEO Dashboard"
version = "1.0.0"
id = "artifacts/aeo-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18277

[services.development]
run = "pnpm --filter @workspace/aeo-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aeo-dashboard", "run", "build" ]
publicDir = "artifacts/aeo-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18277"
BASE_PATH = "/"
