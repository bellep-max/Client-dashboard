# Terraform — generated, never committed
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Keep the provider lock file (.terraform.lock.hcl) committed — it pins versions.
